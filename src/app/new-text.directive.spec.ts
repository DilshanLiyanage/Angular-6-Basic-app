import { NewTextDirective } from './new-text.directive';

describe('NewTextDirective', () => {
  it('should create an instance', () => {
    const directive = new NewTextDirective();
    expect(directive).toBeTruthy();
  });
});
